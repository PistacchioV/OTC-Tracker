# -*- coding: utf-8 -*-
"""Recon de CGD — a casca da tela; o motor é o `recon_cgd.py`.

Bate a lista do FEP contra a posição da B3 do último dia útil (D-1). O motor —
box, DPOSICAO, cadastros, cache com a data da POSIÇÃO — é horizontal e fica
onde está; aqui moram as cinco rotas e as listas de destinatário do e-mail.
"""
