# -*- coding: utf-8 -*-
"""Conf. Matching — a casca da tela; o motor é o `recon_conf_matching.py`.

Bate as operações do FepWeb (o anexo do e-mail `(REPORT) FEPWeb - Operacoes
D-4`) contra os NDFs da Athena (a API do New Deals) do D-1 ANBIMA. O motor —
box, API, Reference Data, cache por data, comentários — é horizontal e fica
onde está; aqui moram as quatro rotas.
"""
