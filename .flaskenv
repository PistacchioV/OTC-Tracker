# Flask CLI defaults (non-secret) — auto-loaded by `flask run` when python-dotenv
# is installed. Lets `flask run` work without `set FLASK_APP=run.py` every time.
# Secrets stay in .env (not here).
FLASK_APP=run.py
FLASK_RUN_HOST=0.0.0.0
FLASK_RUN_PORT=8050
