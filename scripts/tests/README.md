# `scripts/tests/` — verificações de regressão

Scripts autocontidos, sem framework. Cada um imprime `ok`/`FAIL` por asserção e sai com
código **0** (tudo passou) ou **1** (falhou), então servem tanto para rodar na mão quanto
para encadear.

Rodam de qualquer diretório: a raiz do repo é resolvida a partir do próprio arquivo
(`scripts/tests/` → `..` → `..`).

```bash
source .venv311/bin/activate
python scripts/tests/check_tickets.py
```

Nenhum deles encosta em dado real: o arquivo de tickets vai para um `tempfile`, o DuckDB é
recriado num tmp, o Outlook e o SMTP são stubados. Nada sai da máquina.

| script | o que protege | rodar quando mexer em |
|---|---|---|
| `check_boxparse.py` | **paridade JS ↔ Python** do parser de booking recap — executa o JS de verdade no JavaScriptCore e compara campo a campo | `otc_boxparse.py` **ou** `static/js/pages/otc-fileupload.js` (§157) |
| `check_boxsched.py` | varredura agendada do box: dedup `Deal`+`Acronym`, amend preservando o B3 ID, e-mail sem deal não arquivado, roteamento por produto | o bloco de box scan em `routes.py` (§157) |
| `check_cancel_remove.py` | `_nd_cancel_in_file`: cancelado apaga a linha, **exceto** `Success` com B3 ID, que vira `Canceled` e fica à vista — e **quem conta como cancelado é só o `isCancelled`**: `isDead` é importado normalmente | `_nd_cancel_in_file`, `_api_rec_is_cancelled` ou os pulls da API (§156/§173) |
| `check_spb_status.py` | Recon Pay/Rec: só linha com coluna A = `Sucesso` entra na recon, **nas duas trilhas** do HistoricoMensagens | `_cli_spb` em `recon_payrec.py` (§160) |
| `check_tickets.py` | Support Center ponta a ponta: CRUD, as seis regras de permissão, ID sequencial, transição do e-mail de encerramento, JSON corrompido · e a **visibilidade por MESA** (§262): quem é do BO vê a fila do BO, a outra mesa não vê nada dela (sem esse, "todo mundo vê tudo" passaria em todos os outros), ver não é poder (o colega não edita, não comenta e não apaga), papel vazio não vira mesa, e o ticket sem `requester_role` gravado volta pelo cadastro de usuários | `otc_tickets.py`, as rotas `/api/tickets*` ou os templates de ticket (§161) |
| `check_notif_page_url.py` | O de-para **rótulo → página** das notificações, que existe **três vezes** (`_NOTIF_PAGE_URL` no routes, `PAGE_URL` no topbar e no `sw-push.js`): os três com as mesmas chaves e destinos, todo destino sendo página do menu, e **rótulos distintos para páginas distintas** — duas telas com o mesmo rótulo mandam o clique para a errada *e* escondem o aviso de quem só tem a outra liberada | `_NOTIF_PAGE_URL`/`_NOTIF_DS_OTHERPUB` em `routes.py`, `PAGE_URL` em `partials/topbar.html` ou em `static/js/sw-push.js` (§211) · o **par (ação, página)** de cada notificação — `page` é o DESTINO do clique, e a Recon FXO gravava a página do Pay/Rec — e a tradução do par antigo nos **três** lugares (`_notif_page_url`, topbar, service worker), porque o que está no banco não se reescreve (§230) |
| `check_notif_sid.py` | `notifications.target_sid`: a migração numa tabela **sem** a coluna e o isolamento do sino nos três alvos (SID · papel · broadcast) | `_create_notification`, `_push_notify` ou `api_get_notifications` (§161) |
| `check_ds_operacoes.py` | O filtro do arquivo de **operações** do Daily Settlement: as contas do Banco que entram (a PRÓPRIA e a de CLIENTE 2; a de CLIENTE 1 fica de fora), a comparação por DÍGITOS (a conta chega ora `73760.20-5`, ora `7376020 5`), os tipos de título, e a coluna dos filtros sendo **1-based** — a do `_CETIP_BEHAVIOUR`, logo acima no mesmo arquivo, é 0-based, e trocar uma pela outra filtra pela coluna vizinha sem erro nenhum. O que não passa por aqui não existe para a página Operations B3, a mensageria, os avisos nem as recons | o spec `operacoes-jpm`/`operacoes-mgt` do `_DS_IMPORTS` ou o `_ds_process` |
| `check_b3_pattern.py` | notação `"MY"`/`_` do B3 Code e o padrão `YYMMDD` dos arquivos CETIP — inclui **paridade com as duas cópias JS** e a prova de que os 12 markets PREFIX emitem o mesmo código de antes | `split_b3_pattern`/`build_b3_code`, o seed de `commodities-b3`, `_cetip_rules` ou `_CETIP_BEHAVIOUR` (§164) |
| `check_cem_sheets.py` | Accrual/CEM: as abas são lidas por **posição** (1ª summary, 2ª Kapital CETIP), a inversão 228/199 e o erro explícito quando falta a 2ª aba | `_acc_parse_cem_factors` ou `_acc_read_sheets` (§165) |
| `check_cetip_bacc.py` | **BACC** (3º TO do Save CETIP Files): o recorte do intragrupo exigindo os DOIS lados (`and`, não `or`), o `avoid` que impede `'parte (conta)'` de casar com `'contraparte (conta)'`, o zfill(8) que recupera o `41007` do Lawton, o par de colunas que não resolve deixando o arquivo **fora** do anexo, e o **merge** das três listas de TO (um POST sem uma chave não apaga aquela lista) | `_cetip_bacc_copy`/`_cetip_bacc_col`/`_cetip_acct_key`/`_cetip_merge_recipients`, o `bacc` do `_CETIP_BEHAVIOUR` ou o campo no `control-panel.html` |
| `check_publisher_ndf.py` | Publisher × B3: linha **sem Match Tokens casa só pelo texto completo**, `NOTES = BACEN` roteia para Vanilla, e o roteamento antigo (`!= 'PTAX'`) continua valendo com o seed padrão | `_ndf_publisher_row`, `_ndf_publisher_is_bacen` ou o roteamento em `_ndf_deal_from_api` (§166) |
| `check_quote_type.py` | Tipo de Cotação e Fonte de Informação saem do cadastro (Commodities × B3) e **coluna vazia devolve o valor histórico**; inclui o casamento do subjacente contra o padrão `"MY"` e a **paridade com a cópia do navegador** (`b3-quote-config.js`) | `_b3_quote_cfg`/`_b3_code_matches`, os builders do Conecta ou `static/js/b3-quote-config.js` (§177) |
| `check_quoted_in_cents.py` | Quoted in Cents: a divisão por 100 **não olha a moeda** do strike — varre os quatro caminhos (Conecta e Intrag × NDF Comm e Opt Comm) mais as duas cópias no navegador atrás de qualquer termo de moeda na regra | `div100=`/`_cents`/`strike_effective` em `routes.py`, o `buildConectaFields` das duas páginas ou `_is_cents_factor` (§172) |
| `check_co12_roll.py` | Ticker CO1-2 (Brent rolling): em **dezembro** as duas últimas Datas de Verificação são do 2º futuro, nos demais meses só a última — com a frase de dezembro fixada byte a byte, o recuo em dias úteis e os futuros vindos do settlement | `_conf_co12_text` ou os builders de linha das confirmações de Termo/Opção (§178) |
| `check_conf_optcomm_brl.py` | Confirmação de Opção de Commodities em BRL: os dois documentos (USD × BRL) seguem **idênticos palavra a palavra** fora do cabeçalho do Anexo I, o cabeçalho bate célula a célula com o Word, e o **PDF usa o mesmo cabeçalho do template** | `opt-comm-strike-{usd,brl}.html`, `opcao_anexo_heads`/`opcao_pdf` ou `_CONF_OPT_FAMILY_TEMPLATES` (§171) |
| `check_conf_optcomm_palmoil.py` | Confirmação de Opção de **palm oil**: fora de **doze** diferenças declaradas o texto é o do irmão em USD, o Anexo I tem as **19** colunas do Word (cabeçalho, linha e painel de edição na mesma ordem), o **Anexo II** existe e é o mesmo do Termo de palm oil (o `.doc` cita o anexo e não o traz), e o PDF sai do **HTML renderizado**, não da réplica em reportlab | `opt-comm-palmoil-strike-myrusd.html`, `_conf_opt_generation_page`, `_CONF_OPT_FAMILY_TEMPLATES`/`_CONF_OPT_PDF_FROM_HTML` |
| `check_amend_counterparty.py` | Amend da API: SPN/Client/Tax ID são comparados e aplicados, o deal é reencontrado **pelo Deal ID** quando o Client muda (sem duplicar a linha) e **`Sent` e `Success`** só voltam para a fila se algo econômico mudou — troca de book ou contraparte reresolvida destaca a célula e mantém o status (e o Checker), troca de entidade e notional novo derrubam para `Amend`, e os demais status continuam caindo sempre. Confere também `AMEND_FIELD_COLS` × `COL_TO_JSON_FIELD` campo a campo | `_nd_api_amend`, `_ND_AMEND_SKIP`, `_nd_amend_find`/`_nd_amend_index` ou os `AMEND_FIELD_COLS` das páginas (§176) |
| `check_counterparty_lookup.py` | A ordem da busca da contraparte (accronym → identidade da Legal Entity → SPN da API), as duas armadilhas que já shiparam contraparte errada (Settlement Location não vira LE; perna interna não usa o SPN da API), o upgrade do `le-spn` e o badge/filtro Missing Counterparty na tela | `_ndf_ref_by_accronym`, `_ndf_le_refdata`, `_le_spn_upgrade`, os builders de deal da API ou `missing-counterparty.js` (§174) |
| `check_api_links.py` | Link da API virou cadastro: o **seed reproduz a URL histórica byte a byte**, `product`/`date` são sempre do código (um `product=NDF` esquecido na linha não desvia o pull de FXO), e sem cadastro o New Deals cai no endereço antigo enquanto o Unwinds falha pedindo registro | `athena_api.build_url`/`registered_url`, o seed de `api-links` ou os call sites do pull (§173) |
| `check_payrec_run.py` | Pay/Rec com **um botão só**: a dropzone é que decide a fonte (anexos × pasta de insumos) e as **duas cópias da regra** concordam — o teste chama `_gather_sources` de verdade e prova que `manual` sem anexo cai na pasta. Prende também o **IR regressivo do SWAP** no lado interno (bruto → líquido): a alíquota pelo prazo Trade Date → data da conciliação, só no pagamento, sobre o **net do trade**, banco isento pelo cadastro, e sem Trade Date o valor fica bruto | `_gather_sources` em `recon_payrec.py`, o `run()` de `reconciliation-payrec.js` ou a toolbar da página (§180) |
| `check_ndm_pending_sched.py` | Aviso de pendências do **Deals Monitor** (19h00/19h30 BRT): o dia normal sem repetição, o **restart depois do horário** recuperando os slots, e — o que estava quebrado — o horário **não ser queimado** quando o envio falha (SMTP fora, destinatário em branco): o slot é devolvido e a volta seguinte do laço retenta, sem duplicar. `empty` continua sendo desfecho legítimo e mantém a reserva. Prende também o **status publicado** (último desfecho + próximo horário) que o card do Control Panel lê e o envio disparado **de dentro de uma thread, sem request** — que é o caminho do scheduler e o que quebrou com *Working outside of application context* (§222) | `_ndm_pending_disparar`/`_ndm_pending_release_slot`/`_ndm_pending_status`/`_ndm_pending_scheduler_loop` ou o card Deals Monitor de `control-panel.html` (§207) |
| `check_control_panel_sections.py` | As **seis seções** do Control Panel: todo card do registro está no template e vice-versa (card fora do registro é rotina sem dono no `/page-access`), todo card cai dentro de uma seção e toda seção tem card, os três rótulos de cada seção existem nos **três** idiomas, a **ordem** do registro é a da tela (a checklist do `/page-access` sai dela), e o JS de acesso não tem mais o mapa card→grupo escrito à mão — a seção sai do DOM, e o que se esconde é o `.cp-reveal`, não a coluna (a empilhada carrega dois cards) | `_CONTROL_PANEL_CARDS`, as seções de `pages/control-panel.html`, o bloco *Per-user card access* ou as chaves `cp-sec-*` das traduções (§285) |
| `check_import_window.py` | Janela **08:00–20:00 BRT** dos três schedulers de importação (API de NDF, API de FXO e box de commodities): o intervalo de cada um continua o dele e o que a janela decide é se o tique faz alguma coisa. Prende o parser do cadastro (valor malformado devolve `None` e deixa a janela **sempre aberta** — um `.env` errado não pode desligar a importação em silêncio), as **duas pontas inclusivas** (o tique das 20h em ponto ainda importa), a janela que atravessa a meia-noite, e — por AST — que os três laços consultam a janela **antes** do trabalho: dentro do `try` o poll já teria custado a ida à Athena | `_import_window_open`/`_parse_hhmm_window` ou qualquer um dos três `*_scheduler_loop` (§283) |
| `check_export_advanced.py` | O **Advanced Export** (`export-advanced.js`): o INTERVALO DE DIAS — as duas pontas inclusivas, a virada de mês/ano/bissexto, o intervalo invertido devolvendo nada, os dias pedidos **em série** (em paralelo, três meses abririam noventa requisições sobre o processo único que serve a mesa), o dia que falha reportado e o dia VAZIO **não** contando como falha (é dia sem movimento), a Reference Date encabeçando cada linha e a cauda da linha da página ficando de fora —, o endpoint por tela (`recon_date`/`data` das recons contra o `date`/`rows` do resto; sem `daily`, o intervalo fica desabilitado) e o texto da célula como o export a vê (badge → o texto escrito nele). Roda o JS de verdade no JavaScriptCore | `apps/static/js/export-advanced.js` |
| `check_config_names.py` | O **`_REQUIRED_CONFIG_NAMES`** do `apps/__init__.py`, que faz `create_app` recusar subir com um `config.py` anterior ao código. Varre o fonte por AST e prende as duas formas de a lista envelhecer: `Config.<NOME>` que **não existe** no config, e — o caso da §308 — nome lido no **TOPO de um módulo** (isto é, no import, que é o que derruba a subida) e **fora** da lista. Confere ainda que a mensagem nomeia o arquivo, lista todos os nomes que faltam de uma vez e traz o comando que conserta mais o lembrete do restart; e que o `DATABASE_DIR` continua sendo lido no import, senão a varredura passaria por vacuidade. Guarda ainda que **nenhum módulo versionado de `apps/` escreve raiz de rede à mão** (`I:\...` ou `\\servidor\...`): o `SHARED_DRIVE_ROOT` só vale para quem pergunta a ele, e o literal deixaria aquele caminho na letra mapeada na instância que já fala com o UNC | `_REQUIRED_CONFIG_NAMES`/`_require_config_names` em `apps/__init__.py`, ou uma chave nova do `apps/config.py` lida direto do `Config` por outro módulo (§308) |
| `check_about_page.py` | Página **About**: renderiza pelo endpoint e prende as quatro formas de ela envelhecer sem quebrar — **link de carta em 404**, **ícone que não existe** no pacote Tabler (fica só o espaço em branco), **chave `data-lang` sem tradução** (a carta sai em português no EN/ES) e **feature sem carta** (a lista `MUST` é o que exige manutenção: página nova no menu entra lá também). Confere ainda que o fluxo cobre o ciclo inteiro, até confirmação e liquidação | `pages/about.html`, os três `translations/*.json` ou uma página nova no `sidenav.html` (§206) |
| `check_theme_toggle.py` | Toggle claro/escuro: o tema são **três** atributos (`data-bs-theme` + `data-menu-color` + `data-topbar-color`) e o botão do topbar tem de trocar os três, passando pelo `LayoutCustomizer` — mais a classe que suprime as transições, conferida nos dois lados (JS e CSS) | `initThemeToggle` em `visual-refresh.js`, `changeTheme`/`_adjustLayout` em `app.js` ou as regras de logo em `_layout.scss`/`_topbar.scss` (§179) |
| `check_fi_ter.py` | Geração TER pelo cadastro (File Interpreter): linha nova **byte a byte** igual ao golden `_legacy_` nos três produtos (FWD Start, Other Publisher, NDF Commodities), linhas tipo 2, headers dos cinco arquivos, e a prova de que **Fixed editado no cadastro muda a linha** | `_generic_ndf_ter_line`/`_ndf_comm_ter_lines`/`_ter_file_header`, o motor `_fi_build_line` ou `termo-multiclasses.json` (§238) |
| `check_fi_calc.py` | **Fórmulas cadastráveis** do File Interpreter (`_fi_calc_value` + o hook do `_fi_build_line`): as cinco funções (FIELD/DATE/BIZDIFF/ADDBIZ/LOOKUP) com o campo casado pelo nome da coluna da página (cego a caixa/espaço) e o BIZDIFF zero-padded pela largura do format; texto livre devolve None (vale o gerador — é o que mantém o cadastro existente byte a byte); fórmula vence gerador e Fixed vence tudo; o valor efetivo (`_fi_effective_seq_value`) na ordem Fixed > fórmula > gerador; e a cópia do navegador (`FiTer.calc`) concordando com o servidor via `jsc` | `_fi_calc_value`/`_fi_deal_get`/`_fi_effective_seq_value`, o hook `deal=` do `_fi_build_line`, `fi-ter-pair.js` ou o builder de fórmula do `/file-interpreter` |
| `check_fi_variants.py` | **Variantes por par de pernas** do File Interpreter (`base_key`/`le_pair`): sem variante tudo é byte a byte o base (par sem cadastro, chamada sem par), com variante o deal cujo par casa usa o Fixed dela (conta, Participante do header, `file_name`) e os demais seguem no base; resolução cega a caixa/espaço/× e por página; bloco ausente na variante cai no bloco do base; e a cópia do navegador (`fi-ter-pair.js`) concordando com o servidor via `jsc` (pulado sem o binário, como o boxparse) | `_fi_variant_key`/`_fi_variant_file_name`/`_ter_le_side`/`_ter_le_pair`, o `le_pair` de `_fi_build_line`/`_ter_file_header`, `fi-ter-pair.js` ou o modal Add Template do `/file-interface` |
| `check_lp_counterparty_name.py` | A coluna de **CPF/CNPJ da contraparte** das três Live Position (NDF · Option · Swap Characteristics) mostrando o **nome** do RefData: vazio continua vazio, documento sem cadastro volta como número mascarado (apagá-lo esconderia quem falta cadastrar), o **zero à esquerda normalizado dos dois lados** (158 dos 553 cadastros começam com zero), a coluna da PARTE intacta, e o acoplamento com os **dois Settlement Advice**, que leem essa mesma célula para resolver o cliente da conta omnibus (§197) | `_lp_taxid_key`/`_lp_cpty_name_by_taxid`/`_lp_cpty_by_taxid`/`_lp_is_taxid`, `_lpndf_collect`/`_lpopt_collect`/`_swapchar_collect` ou os dois `*adv_collect` (§291) |
| `check_row_action_buttons.py` | A spec **`.ops-row-act`** no Index B3 Results e no Reference Data: squircle travado nos **dois** eixos (só a largura deixa o tema esticar a altura), ícone em 1rem **e** sem o `.fs-13` do tema no markup (ele é `!important` e vencia a regra da página, deixando o ícone com 13 px), ordem e cores por função, tooltip colorido com o CSS presente na página, a inicialização **delegada** do Index B3 Results (os `<td>` são reescritos a cada redraw) e o par Save/success + Cancel/**secondary** dos modais | `.btn-act`/`actionBtns` em `index-b3-results.html` ou `reference-data.html` (§290) |
| `check_holiday_calendars.py` | **Holidays Calendar**: o registro de calendários alimentando as quatro superfícies da tela (pills, `<select>`, cores e CSS), o seed do servidor **campo a campo** igual ao fallback do navegador, o template sem lista escrita à mão, o parser da planilha (coluna A a data nas duas formas que o Excel produz, coluna B a descrição, cabeçalho descartado por **não ser data**), o slug seguro (ele vira caminho em disco), a cor saindo da paleta sem repetir, o `/api/holidays/save` resolvendo pelo registro e o rótulo do sino nos três mapas | `_holiday_calendars`/`_holiday_rows_from_sheet`/`_holiday_cal_slug`/`_holiday_pick_color`/`api_holidays_calendar_create`, `apps-holidays-calendar.js` ou `holidays-calendar.html` (§288) |
| `check_b3_accounts.py` | Cadastro **B3 Accounts** e o **Participante** do header TER: as sete contas do documento da B3, o tipo como `select` e cego a caixa/acento (`Própria` ≡ `OWN`), **guarda-chuva é o TIPO e não estar na tabela** (a conta PRÓPRIA agora está cadastrada e não pode resolver cliente pelo CNPJ), o Nome Simplificado saindo do cadastro pela LE da visão com o header **byte a byte** igual ao de sempre, a LE sem cadastro levantando erro em vez de arquivo com campo em branco, e o `upgrade` do formato antigo. Prende também a coluna **MESSAGING** (§306): de que VISÃO a mensageria sai — Banco assina, MGT/Lawton/Atacama não —, a conta FORA do cadastro gerando (é a de terceiro, e travá-la calaria a rotina onde ninguém abriu o /mapping), a comparação por dígitos, o `upgrade` completando a coluna pelo SEED sem pisar no que foi escolhido na tela, e — por leitura do fonte — que o endpoint pergunta ao cadastro em vez do par fixo e que o inner join com o `opb3-events` continua lá | `_b3_is_omnibus`/`_b3_participant_name`/`_b3_account_type`/`_b3_accounts_upgrade`/`_ter_file_header`/`_b3_msg_view_use`, o registro `b3-accounts`, o `api_opb3_mensageria` ou o campo 4 do `termo-multiclasses.json` (§287/§306) |
| `check_fi_opc.py` | Geração OPC pelo cadastro: FXO e Opt Commodities byte a byte contra os goldens (vanilla, asiática, pernas internas, campos vazios), forma da linha (63 tokens, vazio no fim) e o Fixed editado saindo na linha | `api_fxo_send_conecta`, `api_send_conecta` (opt-comm), o motor ou `opcoes-flexiveis-vcp.json` (§238) |
| `check_fi_taxacambioter.py` | TAXACAMBIOTER pelo cadastro: linha banco/Lawton/swap com **86 chars** byte a byte, preview com rótulos do template, headers pelo motor e a soma das larguras = `record_length` | `_ndfop_conecta_fields`/`_ndfop_conecta_header` ou `taxacambioter.json` (§238) |
| `check_fi_accrual.py` | Accrual pelo cadastro: os ramos de views (BANCO/LAWTON/ATACAMA) e pernas 00/01 byte a byte, PU em branco = 22 espaços, larguras somando 77 | `_acc_swap_records`/`_acc_swap_header` ou `swap-atualizacao-pu-fator.json` (§238) |
| `check_fi_mid.py` | MID pelo cadastro: books com espelho (CEM→LAWTON, EDG→ATACAMA, HYB), sinal negativo, MtM zero → 0.01, larguras somando 93 e o preview fatiando a linha pelo cadastro | `_mtm_swap_line`/`_mtm_swap_header`/`_mtm_fi_registro_fields` ou `mid-informacoes-derivativos.json` (§238) |
| `check_ops_trade_swap.py` | Trade Level (Other Products Summary), linha de **SWAP**: o join das **cinco fontes** por código, o dedup do swap que chega uma vez por Tipo Operação, a tabela de IR contra a fórmula da planilha (incluindo o vão do 721) e a **ordem das colunas nas três listas posicionais** (`<th>` · `rowMaker` · `_OPS_TRADE_COLS`), e o **nome da contraparte saindo do SPN**, nunca do texto livre do arquivo: `_athena_settlements` resolve por `le-spn` → Reference Data (ignorando zeros à esquerda) e é a MESMA coleta da página Swap Athena, do Settlement Advice e do Trade Level — três resoluções seriam três nomes para o mesmo cliente | `_ops_swap_trade_rows`/`_ops_swap_ir_rate`/`_ops_swap_pos_terms`, os seeds `opb3-events`/`swap-ir-*` ou a tabela Trade Level de `other-products-summary.html` (§182/§213) |
| `check_ops_trade_equity.py` | A operação de **EQUITY** no Trade Level e no Settlement Advice. A B3 a registra como SWAP, então a linha já existe — o que falta é o lado do Athena, que é **só de CEM**. Prende a rota **Operations B3 (Título) → Latam (`CLEARING_TRD_ID_INT`/`CLNT` → `Deal_Ref`) → OTM (`270WI`/`270WC`)**, a coluna de clearing que casou decidindo a perna, **Curva Banco = positivos / Curva Cliente = negativos / Resultado Bruto = a soma**, o nome saindo do Reference Data pelo `Cpty SPN` (nunca do `SAFRABM` da B3), o Type **trocado** pelo ativo subjacente — com a **cadeia de fallback** até o `Instrument_Name`, porque as colunas de subjacente do Latam vêm vazias no swap de equity e a coluna saía em branco —, o prazo do IR vindo do `Trade_Date` do Latam, a perna interna ficando no Trade Level **e entrando no Settlement Summary marcada como `internal` no payload** (é liquidação, não aviso — a marca é de servidor e não aparece na tela) enquanto sai do Advice e do e-mail de TED — e o **swap de CEM continuando idêntico** | `_ops_equity_link`/`_latam_equity_b3_index`/`_ops_is_internal_cpty`/`_ops_swap_trade_rows`/`_swadv_collect`/`_opssum_rows`, ou as colunas do Latam Desk Position (§227/§229/§234) |
| `check_ndf_advice.py` | Settlement Advice de **NDF Commodities**: o universo de três peneiras (Resgate · TER · Type COMMODITIES), o join do OTM pelo **sufixo depois do hífen** (os prefixos diferem entre os sistemas), o IR de 0,005% só quando o banco paga com LAWTON isenta, a Cotação Mercadoria da asiática virando `Média Fev/2027`, as linhas no **Trade Level** (TERMO/COMMODITIES, com o IR vindo de um cadastro só para as duas telas) a **quebra dos avisos** (net type × commodity, esta só para quem está registrado) e a **conta omnibus** (o cliente saindo do CNPJ, não do nome do guarda-chuva) | `_ndfadv_collect`/`_subjacente_map`/`_ndfadv_otm_by_suffix`/`_ops_ndfc_trade_rows`/`_ndfc_ir`, `otc_emails.build_ndfc_settlement_emails`, `_b3_is_omnibus`/`_refdata_by_taxid` ou `other-products-ndf-settlement-advice.html` (§194–§197) |
| `check_swap_advice.py` | Settlement Advice de Swap: o universo **compartilhado** com o Trade Level (`_ops_swap_settling`, uma implementação só), a Data de Operação do **trade** (termo antes de início — errar sobe a alíquota do cliente), o IR encolhendo o líquido nos dois sinais, branco ≠ 0%, a página **ligada** (rota, endpoint, `data-api`, sidenav sem âncora morta), o **indexador** saindo da posição com o VCP resolvido pelo `Nome Tipo/Classe` da perna certa, o **aviso impresso** (colunas a partir de Número de Contrato, R$ em BR, assunto e coluna de prêmio), e as **traduções de código virando cadastro** (`swap-index` apontado para o próprio SwapIndex.json, funcionalidade, amortização, sinal e Sim/Não) com o comportamento idêntico ao que estava hardcoded | e a **coluna de Actions** (Edit/Confirm/Delete): opt-in por `data-actions` — as sete páginas irmãs que usam o mesmo visualizador seguem sem ela —, os seis literais do offset das colunas fixas virando `LEAD`, e a edição valendo na tela **e no aviso impresso** (números crus sincronizados, parser tolerante aos formatos US e BR) | `_swadv_collect`/`_swadv_items`/`_swadv_apply_edits`/`_swadv_email_rows`/`_swadv_indexador`/`_ops_swap_settling`, `otc_emails.build_swap_settlement_emails`, `live-position-swap-characteristics.js` ou `other-products-swap-settlement-advice.html` (§184/§187/§188/§208) |
| `check_ops_summary.py` | Settlement Summary (Other Products), porte do NDF: o IR **encolhendo** o caixa nos dois sinais, Total Net × Pay/Rec, o **cruzamento** da conta (banco PAY → `DEFAULT_RECEIVE` do cliente), o net que **não** atravessa produto/LOB, a observação persistida, a prova de que a regra vem das funções `_ndfsum_*` em vez de ser uma segunda cópia, e o **diagnóstico do batch** (qual arquivo falta **e**, quando não falta nenhum, quantas linhas de SWAP havia e quais `Tipo Operação` estavam no arquivo — mais o espaço duplo que fazia a linha cadastrada não casar), a **paridade de Actions/Status com o NDF Summary** e a **junção posição × fluxo pelo Código do Contrato** (era pelo Código Identificador, que no arquivo real é o LOB e apagava todo Cashflow do dia) | `_opssum_rows`/`_opssum_key`/`_OPS_SWAP_JOIN_TOKENS`, os helpers `_ndfsum_net_type`/`_ndfsum_account_fmt`/`_ndfsum_obs_auto` `_ops_batch_status`/`_OPS_SOURCES`/`_ops_norm_event` ou a tabela Settlement Summary de `other-products-summary.html` (§183/§185/§186) |
| `check_opb3_mensageria.py` | **Mensageria** do Operations B3: o assunto de swap (`Vencimento Swap` para os diferenciais, `Prêmio Swap` intacto), o **agrupamento** de amortização + juros da mesma contraparte num e-mail só (com a tabela ainda mostrando os dois `Tipo Operação`), a prova de que assunto e agrupamento leem a **mesma** função, e o **"Favor considerar"** ganhando lado interno para vencimento de swap e termo de commodities — comparado contra o TOTAL do grupo, porque o mapa traz o valor do contrato | `otc_emails.opb3_msg_is_swap_venc`/`build_opb3_mensageria_email`, o agrupamento de `api_opb3_mensageria` ou `_opb3_internal_swap_map`/`_opb3_internal_ndfc_map`/`_opb3_internal_trade_map` (§202) |
| `check_opb3_events.py` | **Operations B3 → liquidação**: o cadastro `opb3-events` (Tipo Título × Tipo Operação × Status B3 × Consider/Disregard) com precedência, coringa e casamento que ignora pontuação (`CANCELADA: COMANDADA` ≡ `CANCELADA:COMANDADA`), a prova de que as **cinco** telas de liquidação passam pela mesma função (e que a página Operations B3 **não** filtra), e a **direção do valor** no intragrupo — a linha do B3 escolhida por (Título, Conta, Conta Contraparte), sem o que metade dos JPM × MGT saía com o sinal invertido | `_opb3_settle_ok`/`_opb3_event_rules`/`_opb3_settle_rows`/`_ndfsum_b3_legs`/`_ndfsum_b3_val` ou o seed de `opb3-events` (§213) |
| `check_pc_mass_update.py` | **Pending Confirmation › atualização em massa**: as colunas que ficam de fora (as quatro derivadas e o Trade Number, que é a chave da linha), o tipo do campo por coluna (máscara de data, dropdown, autocomplete do Reference Data), a **cascata** de recálculo (SPN/Client → identidade; Trade/Maturity → prazo e aging) e a prova de que o recálculo é do **servidor**, reusando a mesma regra da importação do Pending Update | `_pc_derive_row`/`api_pending_confirmation_derive`, `_pc_signature_status` ou o bloco `PC_MASS_COLUMNS`/`PC_CASCADE` de `pending-confirmation.html` (§214) |
| `check_manual_conf.py` | **Manual Confirmations**: quem valida cada produto (cadastro `manual-conf-validation`, Produto × LOB com LOB em branco como coringa; sem cadastro cai em OTC + MO **e a tela avisa**), **MO e FO em PARALELO** (a linha parada nas duas aparece nos DOIS cards), o `Pending` e o `Aging` **derivados** e não digitados, o **reject** limpando o que já foi validado e devolvendo ao OTC, o carimbo com hora **e SPN da sessão** (o SPN do POST é ignorado), o recorte dos **quatro** produtos que geram confirmação — pelo `source`, porque as três páginas de NDF gravam o mesmo Product Type — e **qual mesa assina cada etapa** (OTC→BO, MO→MO, FO→FO; ADMIN não é passe livre, e rejeitar segue a mesma regra) · o **rótulo em inglês** de toda coluna (o mapa é completo: coluna sem entrada apareceria com o nome do banco) · o **`Notional Amount CCY`** — moeda do campo daquele produto (Strike Currency em mercadoria e FXO, Quantity Currency nos NDF genéricos) e a repartição em (moeda, valor) · o **anexo do BACC**: os dois cortes (sem Data Callback e Pending ≠ Ok), a ordem por Aging decrescente, as doze colunas na grafia pedida e os tipos que chegam ao xlsx (valor como NÚMERO com máscara de milhar, Aging sem máscara, texto que não parseia sem máscara nenhuma) · e o **E-mail Subject** saindo do recap da pasta: casamento pelo Trade ID no nome do arquivo, recap único valendo para o grupo, e NADA escrito quando há vários sem nome de operação (§258–§261) | `manual_conf.pending_stage`/`rule_for`/`reject`/`monitor_payload`, `_mc_save_from_deal`/`_MC_CONFIRMATION_SOURCES`/`_mc_conf_trade_keys` ou as telas `manual-confirmation-monitor.html` / `-track.html` (§217) · e a **grade do Track validando**: a transição vazio→data exigindo a mesa (403), a justificativa fora do prazo (409, dizendo a coluna) e o **carimbo de quem assinou**; editar outra coluna de linha já validada não recarimba, apagar a data apaga o carimbo, e o lote é tudo-ou-nada (§232) |
| `check_mc_notify.py` | **Quem recebe** o aviso de cada etapa da esteira: o destino saindo do `pending_stage` **depois** do carimbo (e não da etapa assinada), Pending MO → MO+BO, Pending FO → FO+BO, `MASTER` em todas e `ADMIN` em nenhuma, `Ok` voltando a avisar todos, os quatro rótulos batendo com as constantes `manual_conf.PENDING_*` (são escritos por extenso — se um divergisse, o aviso voltaria a ir para todos sem erro nenhum), o lote em etapas diferentes unindo os papéis, e o filtro do feed casando a lista `MO,BO,MASTER` **e** o valor antigo de um papel só | `_MC_STAGE_NOTIFY_ROLES`/`_mc_notify_roles`/`_notif_roles`/`_push_notify` ou o filtro de `api_get_notifications` (§231) |
| `check_conf_escalation.py` | **Confirmations Escalation** (card do Control Panel): o grupo de Front Office casando pelo **tipo de confirmação** — `OPTION EDG` não é produto, é `FXO` × LOB `EDG`, e cadastrá-lo como produto faria o grupo nunca casar em silêncio —, o Pending FO **sem grupo** indo para `unmatched` em vez de sumir, a escalação levando só o **último dia** (`left == 0`) e o vencido (nunca a véspera), o `Pending MO/FO` entrando nos dois e-mails, os quatro **assuntos por extenso**, a data de envio para validação com a cadeia de fallback das linhas antigas, `empty` × `no_recipient` como desfechos distintos, o Run individual mandando **um** e-mail só, e segunda/quinta **rolando** para o próximo dia útil ANBIMA (a pergunta é feita ao contrário: que segunda/quinta desemboca em hoje?) | `_ce_snapshot`/`_ce_fo_group_id`/`_ce_run`/`_ce_is_routine_day`/`_CE_FO_GROUPS`, o template `email-template-confirmations-escalation.html` ou o card em `control-panel.html` (§257) |
| `check_recon_fxo.py` | **Reconciliação de FXO** (porte do script da mesa): a base âncora (conta como texto **e** como número — testar só um faz a linha sumir), a chave `CETIP-` ≡ `CETIP_`, a **prioridade do DealID** sobre o MatchingDealID e o MatchingDealID valendo **só quando existe do lado da B3** (ele é a perna do outro lado e quase nunca tem registro na CETIP; sem esse filtro cada valor entra no join `outer` como um `Unmatched Athena` fantasma — a mesma operação repetida pela chave da perna oposta), o corte por **CRITÉRIO** (`fxo-book-disregard`: até três pares `coluna = valor` em conjunção, com a **coluna escolhida** do cabeçalho real — a primeira versão fixava `TRADING BOOK`/`OTHER BOOK`, que o relatório não tem —, par pela metade **não conta**, a linha sem critério nenhum é **ignorada** (senão apagaria o lado da Athena), valor e nome de coluna casados **normalizados**, regra com coluna inexistente **pulada com aviso** em vez de exigir menos, e o formato antigo traduzido no `upgrade`), a perna interna (`INVERT DIRECTION = No` renomeia sempre; `= Yes` só quando Ctpty **e** JPM Dir estão os dois NOK; `USE = Disregard` tira as linhas da Athena **antes** do batimento, cego a pontuação, olhando as DUAS colunas de contraparte — `CounterpartyName` e `MatchingCounterpartyName`, porque a operação intragrupo chega pelos dois lados e é a segunda que a tela mostra —, avisando quantas saíram e sem valer mais como renomeação), os tipos de comparação (número BR, tolerância de 0,67 no prêmio, AAAAMMDD × ISO), o fixing da asiática saindo do cronograma, **'Sem match' vencendo 'NOK'**, e o contrato com a tela (as colunas vêm do servidor) | `recon_fxo.reconcile`/`base_athena_para_match`/`_aplicar_perna_espelhada`/`read_dposicao`, os seeds `fxo-cpty-cnpj`/`fxo-internal-cpty` ou `reconciliation-fxo.html` (§216) |
| `check_pc_widgets.py` | **Pending Confirmation › cards do topo**: cada card diz a que faixa pertence pelo `data-pc-band` (era pela **posição no DOM**, e mover/inserir um card deslocava em silêncio os números de todos os seguintes), o card de **Total** com cor fora da rampa verde→vermelha, a largura de 1/7 vinda do CSS da página (o Bootstrap para em `row-cols-6`) e a contagem rodada de verdade no `jsc`: o Total fecha com a soma das seis faixas, linha a linha, e a linha `Exception *` fica de fora | o bloco `updateWidgets`/`RANGE_ORDER`/`data-pc-band` ou os cards de `pending-confirmation.html` (§215) |
| `check_ndfsum_fwd_rate.py` | NDF Summary › Trade Level: o **Forward Rate com todas as casas** do SETTLEMENT.xlsx (eram 6 fixas) com piso de 6, o **Fixing Rate acompanhando a precisão do forward que o gerou**, a prova de que o cálculo sempre leu o valor **cru** (só a exibição mudou) e a largura/`nowrap` da coluna — sem eles a taxa longa quebra em duas linhas e parece outro valor | `_ndfc_fmt_fwd`/`_ndfc_text_decimals`/`_ndfc_strike_calc` ou as colunas 12/15 de `ndf-summary.html` (§210) |
| `check_summary_glow.py` | NDF Summary: a luz de fundo dos cards de reconciliação (verde = bateu, âmbar = verificar). Prende o que quebra **sem erro no console** — o nome das classes nos dois lados (JS escreve, CSS lê), a ordem do `:hover` contra o cinza de `.ops-widget:hover`, e o tema **claro pesando mais** que o escuro | as regras `.ops-recon.is-ok`/`.is-check` ou o `setRecon()` de `ndf-summary.html` (§181) |
| `check_fwdstart_conf.py` | Confirmação de **NDF FWD Start**: as três colunas próprias do forward start (Pontos de Termo = Strike Set Offset, Data de Verificação da Taxa Forward = Strike Set Date, Data Efetiva = Trade Date), o **Nº = B3 ID** (vazio + aviso antes do registro, nunca o Deal interno), a Taxa Forward **"Não Aplicável"** que liga a cláusula 4.2.l.2, a janela de verificação (First vazio/igual ao Last → Inicial "Não Aplicável"), a Moeda Base sendo a moeda **estrangeira**, o eixo de segregação igual na tela e na geração, e o PDF saindo do **mesmo HTML** do `.doc` | `_conf_fwdstart_rows`/`_conf_fwdstart_moeda`/`_conf_pick_fwdstart`, `confirmation_pdfs.word_html_pdf` ou `confirmations/ndf-fwdstart-strike-me.html` (§203) |
| `check_ndf_pdf_cpty.py` | Ficha em PDF no aviso de NDF: **seed do mapping == tupla de fallback**, cadastro vazio desliga o anexo, arquivo ausente volta à lista histórica, e o match tolera acento/caixa/travessão | `_ndf_pdf_set`/`_NDF_PDF_COUNTERPARTIES` em `otc_emails.py` ou o seed de `ndf-pdf-cpty` (§169) |
| `check_manual_deals_ea.py` | **Manual Deals EA** (card do Control Panel): o Deal do FWD Start que sai é o do **re-booking em vanilla** (o original foi cancelado, e pedir para excluí-lo deixaria dentro do EA justamente a operação que existe) e o par é **gravado** no pull, porque em nenhum outro momento os dois lados se veem juntos; o Other Publisher levando **só contraparte externa** pelo ECONOMIC GROUP do Reference Data — e não pelo nome começar em BANCO, que derrubaria Banco Safra; `empty` (nada a reportar, e por isso não envia) × `no_recipient` como desfechos distintos; a Legal Entity por extenso do `le-spn`; o claim do disparo devolvido quando o SMTP falha; e os dois horários (20:00 / 16:30 BRT) | `_mdea_*` em `routes.py`, `email-template-manual-deals-ea.html` ou o card em `control-panel.html` (§273) |
| `check_optadv_ir.py` | O **IR de 0,005% do aviso de OPÇÃO**: quem decide se há imposto — e quanto — é o NET por contraparte × data (só pagamento de PRÊMIO, e só quando o banco paga), nunca a linha. Prova o caso da Mondelez de 24/08/2026 número a número: apurado `(28.884,17)`, IR `1,44` e Resultado Final `(28.882,73)` — o rateio vai só para as linhas que PAGAM, senão a retenção anda para os dois lados e o rodapé, que soma a coluna, imprime `(28.884,13)`. Cobre ainda net a favor do banco (zero), exercício fora da base, a sobra do arredondamento e a isenção do `ndfc-ir-exempt`. Cobre também a TABELA do aviso: no **Pagamento de Prêmio de Opção** (mercadoria, câmbio e EDG) ela não leva `IR 0,005%` nem `Resultado Líquido` — o imposto é do net e aparece só no quadro de baixo, sem linha de IR quando não há retenção —, enquanto exercício/recompra e o termo de mercadoria seguem com as duas colunas; o corte vem antes da tabela, então a ficha em PDF sai igual ao corpo | `_optadv_apply_ir`/`_optadv_email_rows` em `routes.py` ou `_ndfc_settlement_email` em `otc_emails.py` (§310) |
| `check_ops_print_advice.py` | **Print Advice do Other Products**: um botão no Settlement Summary gerando as TRÊS famílias (Swap · NDF Commodities · Opção) e, sobretudo, o **blocker** — contraparte com Resultado Bruto/Líquido em branco NÃO vira aviso, e o corte é da contraparte INTEIRA porque o aviso é netado por ela; o IR vazio não bloqueia (é derivado, e zero é legítimo); cabeçalho sem a coluna não corta ninguém; o disclaimer atravessa os dois formatos de resposta (corpo JSON e `X-Blocked` em base64 no .zip). Cobre também a ordem do **OTM Settlements** (Pending → New → Ok, por rank e não por texto) | `_opsadv_*` em `routes.py`, `ops-advice-blocked.js`, as três telas de Settlement Advice ou `otm-settlements.js` (§274/§275) |
| `check_recon_comitente_errors.py` | **Recon Comitente — os quatro desfechos de banco**: `DatabaseLockTimeout` → **503 + `retryable`** (ocupado não é quebrado: com 500 a tela desiste e o operador reexecuta a rotina inteira), `TransactionOutcomeUnknown` → 500 declarando `outcome_unknown` e devolvendo o `operation_id` (a gravação PODE ter acontecido — reexecutar às cegas duplica o dia, e a resposta não escolhe por quem opera), `DatabaseCleanupError` → 500 dizendo que a reconciliação **concluiu**, e o genérico sem vazar o `str(e)` na tela. O `FileNotFoundError` continua fora dos erros de banco | `reconciliation_comitente_run` / `_data` em `routes.py`, ou `apps/pages/database_access.py` |
| `check_mt300.py` | **MT300** (card do Control Panel, 19:30): quem entra no e-mail sai do cadastro `mt300` e o casamento tenta **CNPJ → SPN → nome por tokens**, bastando um — no e-mail real a Garoto chega como `LTDA` e está cadastrada como `SA`, e por nome não casaria; o **sinal** vem da DIREÇÃO (o notional é gravado positivo e a venda é negativa no MT300); `Other Quantity` é DERIVADO (quantity × rate) com **seis casas**, conferido dígito a dígito contra o e-mail real de 07/08/2026; e sem operação do grupo o e-mail **não sai** (`empty` ≠ `no_recipient`) | `_mt300_*` em `routes.py`, `email-template-mt300.html` ou o cadastro `mt300` (§278) |
| `check_quotes.py` | **Quotes** (PTAX · Equities · Commodities): as opções vêm do Subjacente do Index B3 separado pelo campo `Classe` (sem vazar classe entre os tipos e sem os inativos), o símbolo sai do **/mapping** e a falta dele vira 404 pedindo cadastro, a PTAX traz **só o boletim de Fechamento** com a data em `mm-dd-aaaa`, o `period2` do Yahoo leva **um dia a mais** (o fim é exclusivo), `None` da fonte vira célula **vazia** e não `0.00`, mais o padrão de tabela da página (filtro antes do `.DataTable()`, `autoWidth`, export completo, `otcCellCopy`) e as três traduções | `apps/pages/quotes.py`, `_quotes_underlyings`/`api_quotes_*` em `routes.py`, `pages/quotes.html` ou `js/pages/quotes.js` |

| `check_ops_equity_option.py` | **A OPÇÃO de equity acha o valor no OTM**: o Resultado Apurado sai do sufixo da `Combinação de operações`, campo que a opção de **ação** não preenche — sem valor, a linha aparecia no Trade Level com a célula vazia e **sumia do Settlement Summary**, que descarta quem não tem o que liquidar. Prende o plano B (o MESMO elo do swap de equity, pelo Título da B3), o SPN vindo junto (é ele que troca o `SAFRABM` pela razão social), a **precedência** — o sufixo, quando existe, vence — e a chegada da linha às DUAS telas | `_optadv_collect`/`_ops_opt_trade_rows`/`_opssum_rows` em `routes.py` (§281) |
| `check_latam_source_pick.py` | **Qual arquivo é o relatório do dia** do Latam Desk Position: vence o **mtime mais recente** (era `sorted(...)[0]`, o mais ANTIGO — o relatório é reemitido no mesmo dia e o import regravava o JSON com a posição da manhã dizendo "sucesso"), o nome só desempata, os preteridos **voltam na lista e ficam em disco** (não se apaga o que não se leu), e o **Save Daily Settlement usa o mesmo seletor** — senão os dois caminhos discordam sobre qual é o relatório do dia | `_latam_pick_source`/`_latam_import`/`api_cp_daily_settlement_save` em `routes.py` (§281) |
| `check_weak_ccy_rate.py` | **Moeda fraca: o strike é invertido UMA vez, na importação.** A perna fraca é achada nas DUAS colunas (qual delas carrega o notional depende de como a mesa bookou), par sem moeda fraca passa como veio e par com as duas fracas não inverte (sem BRL não há convenção). Prende também que o arquivo TER **não inverte de novo** — só arredonda pelas casas do cadastro, com a Taxa a Termo igual nos dois arranjos do mesmo trade — e que `qty × rate` devolve o contravalor que a própria API declara | `_ndf_weak_leg`/`_ndf_deal_from_api`/`_generic_ndf_ter_line` em `routes.py` (§282) |

## Dependência externa

`check_boxparse.py`, o `check_export_advanced.py` e a **seção 5** do `check_quote_type.py` precisam do **JavaScriptCore** (`jsc`), que já vem no macOS em
`/System/Library/Frameworks/JavaScriptCore.framework/Versions/A/Helpers/jsc`. Ele existe
porque `otc_boxparse.py` é a **segunda cópia** de uma regra de negócio que também vive no
navegador — sem executar os dois lados, uma divergência (arredondamento, data fora de faixa)
passa em silêncio e só aparece como número errado na tela. Ver §157.

A **seção 4** do `check_pc_widgets.py` usa o mesmo `jsc` por outro motivo: a contagem dos
cards só existe em JavaScript, então a alternativa seria reescrevê-la em Python — uma
terceira cópia da regra, que é justamente o que o teste deveria pegar.

No Windows da equipe o `check_boxparse.py` e o `check_export_advanced.py` não rodam; o `check_quote_type.py` e o
`check_pc_widgets.py` rodam, apenas pulando a seção de paridade/contagem (ambos avisam na
saída). Os demais rodam inteiros.
